# Public release notice

This repository contains Skill code only. The real SQLite corpus is intentionally excluded by `.gitignore` and distributed through the separate `douyin-tiktok-story-database` repository.

The Skill code is released under the MIT License. The separate SQLite corpus is not covered by this repository's MIT License.
