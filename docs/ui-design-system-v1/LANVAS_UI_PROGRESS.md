# Lavans 全软件 UI 改造：进度与交接

## 当前目标

按已确认的八阶段计划完成 Lavans 全软件 UI 改造。阶段五本地工具与管理页面的视觉接入、自动渲染测试和截图核对已经完成，等待用户在软件浏览器审核。

已确认全软件母版：`D:\Backup\Documents\temu\lanvas-design-system-v1\lanvas-ui-master.html`。一键复色同时精确继承母版视觉与排版；其他页面只继承颜色、按钮、字体、字号、图标、圆角、阴影、材质、状态和动效，不继承母版排版，原页面结构继续冻结。

## 阶段一已完成且可恢复

- 已确认当前分支为 `main`，基线提交为 `98eb28052630b66b46610efed4ff95e4b432d5d4`。
- 已建立恢复检查点：`D:\Backup\Documents\temu\lanvas-ui-checkpoints\stage1-20260821`。
- 检查点包含工作区二进制差异、Git 状态、未跟踪文件清单、24 个关键 UI 源文件副本与 SHA-256 哈希。
- 当前一键复色统一样式标记为“未验收实验版”；不得把它当作阶段三的最终实现。
- 已完成历史视觉规范冲突审计，并新增 `LANVAS_UI_DECISION_REGISTER_V1.md` 作为跨窗口统一执行入口。

## 阶段二已完成内容

- 建立阶段二修改前检查点：`D:\Backup\Documents\temu\lanvas-ui-checkpoints\stage2-before-foundation-20260821`。
- 将原统一覆盖式 CSS 拆为令牌、公共组件、全局外壳和页面专属四层。
- 为 13 个已接入页面建立稳定页面身份；全局导航图标、致敬入口和主题桥仅在 `shell` 运行。
- 删除公共层对 `.sidebar`、`.card`、`.panel`、`.primary-btn` 等通用类名的直接覆盖，阻断 iframe 页面之间的样式污染。
- 保留一键复色现有实验布局规则，但全部限定在 `body[data-lanvas-page="recolor"]` 内。
- API 设置和其他页面没有新增版面位置规则。

## 阶段二验证结果

- JavaScript 语法检查通过。
- 5 个 CSS 文件括号平衡检查通过。
- 13 个接入 HTML 页面全部存在页面身份映射，无遗漏。
- 4 个分层 CSS 导入文件均存在，并由本地服务返回 HTTP 200。
- `index.html`、`canvas-api-settings.html`、`enhance.html` 和智能画布入口均由本地服务返回 HTTP 200。
- 部署后 6 个目标文件与隔离区 SHA-256 完全一致。
- 软件内浏览器未完成：浏览器连接组件先无法访问独立本地端口，随后连接内核报路径错误；本机 HTTP 服务本身正常。此项不得标记为通过。

## 阶段三已完成内容

- 一键复色严格按唯一母版同时继承排版与视觉；所有几何规则均限定在 `body[data-lanvas-page="recolor"]`，不会作为其他页面的排版模板。
- 保留并重做统计栏、模板图与参考色大面积上传区、自动配对、一键生成、连续任务列表、底部三操作、彻底清空、右侧双图预览与系统日志。
- 任务列表取消分页，改为连续滚动；每行和右栏只显示“模板图 → 生成图”，不保留参考色三图灯箱。
- 放大任务缩略图并统一行高；失败原因与任务保持同一行；每项保留独立重做与移除操作。
- 详情灯箱保持“模板图、生成图、参考图”的既定顺序，并保留键盘左右键切换任务。
- 右侧检查栏支持收起与恢复；响应式窗口下同样不覆盖主工作区。
- 系统日志增加“清空日志”，只清空一键复色日志文件，不删除任务、素材、结果，也不触碰画布数据。
- 深浅色继续使用相同结构、文字和图标；一键复色内部不增加独立主题按钮。

## 阶段三验证结果

- `app.js` 与 `logRoutes.js` JavaScript 语法检查通过。
- `lanvas-pages.css` 括号平衡检查通过（126 / 126）。
- `recolor.html`、统一令牌 CSS、页面 CSS 与 `app.js` 均由本地服务返回 HTTP 200。
- 上传、自动配对、生成、彻底清空、日志清空、右栏折叠、任务重做和键盘左右切换需要按最新决定统一入口；旧扫描与重置入口由新流程取代。
- 软件内浏览器页面已请求打开，但自动读取浏览器画面的内核仍报“系统找不到指定的路径”；因此可视验收尚未标记通过。

## 阶段四已完成内容

- 已建立阶段四修改前恢复点：`D:\Backup\Documents\temu\lanvas-ui-checkpoints\stage4-before-shell-20260821`。
- 全局软件外壳统一使用 Lavans 字体、深紫品牌、同系双色图标、磨砂侧栏、选中态、悬停态和深浅主题令牌。
- 保留全局导航的名称、顺序、折叠逻辑与 11 个既有页面入口；没有修改任何路由或 iframe 内页结构。
- 系统设置组继续保留在左侧栏下部；一键复色只保留为全局导航入口，没有增加第二套软件导航。
- 左下角固定“改自大雄画布，致敬原作”，并关联 `https://github.com/hero8152/Infinite-Canvas`。
- 深浅模式沿用同一 DOM、同一文字、同一图标与同一尺寸，只通过主题令牌改变颜色、材质和阴影。

## 阶段四验证结果

- `lanvas-unified.js` JavaScript 语法检查通过。
- `lanvas-shell.css` 括号平衡检查通过（49 / 49）。
- 16 个可见导航名称和 11 个页面入口逐项静态核对通过。
- `index.html`、统一 CSS、外壳 CSS 与统一脚本均由本地服务返回 HTTP 200。
- 软件浏览器页面已请求打开；自动读取浏览器画面的内核仍报路径错误，因此可视验收等待用户查看，未虚报为自动通过。

## 阶段五已完成内容

- 已建立阶段五修改前恢复点：`D:\Backup\Documents\temu\lanvas-ui-checkpoints\stage5-before-local-pages-20260821`。
- 文生图、细节增强、图片编辑、角度控制、在线生图、GPT 对话、项目列表和素材库共 8 个页面已接入 Lavans 统一字体、主题令牌、卡片材质、图标线宽和紫色光带主按钮。
- 所有页面继续使用原 HTML、原页面专属 CSS、原功能名称、原控件位置和原页面结构；阶段五没有向页面视觉规则加入宽高、定位、间距、网格、顺序或其他排版声明。
- 深浅色使用同一页面结构与组件，只改变颜色、材质和阴影。
- 动态插入的按钮只在新插入区域内识别，不再反复扫描整个页面，避免长时间使用后的额外开销。

## 阶段五验证结果

- `lanvas-unified.js` JavaScript 语法检查通过；4 个分层 CSS 文件括号平衡。
- 8 个页面、统一页面 CSS 和统一脚本均由本地服务返回 HTTP 200。
- 8 个页面的 HTML 与页面专属 CSS 均与阶段五检查点 SHA-256 一致，确认本阶段没有修改页面结构。
- 真实 Edge 内核渲染测试通过：8 个页面均使用 MiSans VF、无横向溢出，浅色与深色下页面尺寸和主要区域坐标完全一致。
- 文生图、细节增强、图片编辑、角度控制、在线生图、GPT 对话和项目列表的主操作按钮均识别为统一主按钮；素材库主按钮由其动态内容出现时识别。
- 视觉截图核对完成，保存于 `D:\Backup\Documents\temu\lanvas-ui-checkpoints\phase5-review-20260821`。
- GPT 对话页在静态服务器下会把缺失的后端接口响应解析为 HTML 并报告 JSON 错误；这是静态预览环境限制，不是本阶段 UI 脚本错误，正式软件服务下需继续冒烟验证。

## 当前工作区事实

- 既有用户修改继续全部视为用户资产；本阶段没有重置、清理或覆盖其他文件。
- 阶段五只修改统一页面视觉 CSS、统一页面识别脚本和本进度记录；没有改动 8 个目标页面的 HTML 或页面专属 CSS。
- 没有修改产品功能、页面 HTML 结构、路由、业务 API 或数据。

## 一键复色阶段 F 完成（2026-08-22）

- 已完成已确认的浅色与 G 近黑灰同构工作台：8 个真实快捷入口、隐藏式整卡上传、连续任务列表、右栏双图、底部三操作及统一正式弹窗。
- 已接入参考色管理、真实同批裁剪、复色独立历史、并发设置、提示词／单价批次锁定显示、按需导出、三图缩放和运行中关闭保护。
- 新增阶段 F UI 契约测试 5 项；阶段 B～F 相关 28＋5 项本地测试、前端语法、差异检查和本地浏览器浅色关键界面检查通过。未连接 Provider、未调用付费 API。
- 收尾恢复目录：`D:\Lavans备份\backup_before_recolor_stageF_complete_20260822_165254\`。

## 下一步

当前停在阶段 F 稳定检查点。下一步必须先切换为 SOL＋第一个“极高”，再执行阶段 G：无付费完整回归、本地伪造 Provider 全链路和最终设计验收矩阵总审查。真实最小任务必须由用户另行授权。
