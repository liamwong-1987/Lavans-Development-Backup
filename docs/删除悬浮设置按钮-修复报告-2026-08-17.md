# 删除悬浮设置按钮 — 修复报告

- 日期：2026-08-17
- 项目：Lavans / Lavans（`D:\Lavans备份`）
- 工作流：软件开发团队 · BugFix 快捷路径（工程师删除 → QA 回归）
- 结论：**IS_PASS: YES**（7/7 验证点全通过）
- 提交：`7761ea7`（本地，未 push）

---

## 一、问题

复色界面右上角（顶部「重置」按钮左边）有一个悬浮的「接口设置」齿轮按钮（`api-settings-btn`，`position:fixed; top:12px; right:140px`），用户多次要求删除，一直遗漏。

## 二、根因（为什么之前没删）

前几轮注意力集中在「补全生成参数面板」和「修复上传缓存失败」两个任务上，这个悬浮按钮的删除需求被遗漏，未纳入任务范围。

## 三、删除内容（2 个文件）

| 文件 | 删除内容 |
|------|---------|
| `resources/frontend/recolor.html` | `<button class="api-settings-btn" onclick="openApiConfig()">` 整行 + 上方注释「API 设置入口」 |
| `resources/frontend/style.css` | `.api-settings-btn{...}` 和 `.api-settings-btn:hover{...}` 两段样式 |

## 四、保留项（不删，避免误伤）

- `app.js` 的 `openApiConfig()` 函数（L4069）：`#settings` hash 路由仍在用，保留。
- `app.js` 的 `#settings` hash 路由（L3602）：保留，不报错。
- 画布/创作/online-studio/text-studio 模块对「接口设置」的引用：走画布 API 设置页，与本按钮无关，不碰。

## 五、验证结果（7/7 PASS）

| # | 验证点 | 结果 |
|---|--------|------|
| 1 | recolor.html 无 api-settings-btn | PASS |
| 2 | style.css 无 .api-settings-btn | PASS |
| 3 | 删除后 HTML 结构完整（batch-overview 区标签闭合正常） | PASS |
| 4 | openApiConfig 函数完整未误删 | PASS |
| 5 | #settings hash 路由仍存在不报错 | PASS |
| 6 | 全局无残留引用（仅 *.before-uifix 备份文件命中） | PASS |
| 7 | 其它模块引用无关、未改动 | PASS |

## 六、备注

- 删除后，用户改 Provider 的入口：生成弹窗内直接选 Provider；改 Provider/Key 去画布「API 设置」页。
- 需**重启 Lavans** 后生效。
