# AGENT / API 模型分类修复：阶段 5 最终本地验收

## 目标与边界

- 使用本地假中转站验证五类模型拉取、历史误分自愈、人工分类覆盖、保存与刷新恢复。
- 使用真实 API 设置页面做 2560×1440 浅色/深色浏览器验收。
- 验证 AGENT 相关回归继续保持精确 Provider / 模型绑定、整轮授权和恢复安全。
- 本阶段不读取或修改真实 API Key，不请求真实 Provider，不生成真实媒体，不修改生产画布数据。

## 模型卡

- 推荐模型：Sol。
- 推理强度：high。
- 当前执行者：当前主智能体；未授权子智能体。
- 原因：验收跨越后端分类、前端持久化、AGENT GenerationRound、安全授权、重启恢复和视觉交互。
- 禁止范围：一键复色、真实配置、Provider 自动切换、模型 fallback、依赖、Git 提交与推送。

## 实际结果

1. 假上游返回六个包装模型 ID；真实后端分类接口解包后，页面显示生图 1、LLM 1、视频 1、音频 1、未知 2。
2. 历史配置故意把 `seedance-2.5` 放在 LLM；拉取最新上游分类后，选择器自动显示为视频，应用后从 LLM 移入视频。
3. 人工把 `opaque/manual-model` 从未知改成音频；保存结果写入 `model_category_overrides`，整页刷新后仍位于音频。
4. 保存后内存状态为：
   - 生图：`gpt-image-2`
   - LLM：`deepseek-v4-pro`
   - 视频：`seedance-2.5`
   - 音频：`mureka-v8`、`opaque/manual-model`
   - 未知：`future/model-v1`
   - 人工覆盖：`opaque/manual-model -> audio`
5. 浅色和深色均在 2560×1440 下完成弹窗验收；五个分类标签均可点击，列表只显示对应类别。
6. 相关跨层回归 58/58 通过：多节点 GenerationRound、整轮确认、精确 Provider / 模型、参考图到视频、安全授权、重启恢复、`remote-unknown` 不重发和生产画布往返均通过。
7. 生产画布扫描：44 个文件、181 个节点、80 条连接，修改 0 个生产画布文件。

## 证据

- 本地验收服务：`resources/backend/tests/canvas-api-model-category-visual-server.js`
- 浅色截图：`outputs/agent-api-model-stage5/light-five-categories.png`
- 深色截图：`outputs/agent-api-model-stage5/dark-five-categories.png`
- 本地验收服务 SHA-256：`750194A79E5A33798CC5E30969B99B1601FA3118EB322D73A704DA01DA0474DA`
- 浅色截图 SHA-256：`8701EE4B0C4F3F9287CD4BF07E8B9D34842260EB82FFF9A96FBE0F46C531565A`
- 深色截图 SHA-256：`48F6F1966E1799B008AB78253C72D8376D606D9F2AE08093EB853A975BBCB5D3`
- `node --check`：通过。
- `git diff --check`：通过；只有仓库既有 LF/CRLF 提示，无空白错误。

## 下一断点

阶段 5 本地验收完成。下一阶段按用户新授权做四类真实最小请求：图片、视频、音频、LLM 各一次；必须使用已保存的明确 Provider、模型和最低可验证规格，不自动切换或重试。任何一类出现未知远端结果时标为 `remote-unknown`，不得自动重发。
