# AGENT 四类模型真实验收：阶段 6 预检

## 目标与边界

- 在任何付费生成前，确认 APIMart 目录里的真实模型 ID 和图片、LLM、视频、音频分类可信。
- 确认 Lavans AGENT 是否真的具备四类 Provider 请求与原生节点落地链路。
- 本预检没有发送 LLM、图片、视频或音频生成请求，没有消耗生成额度，也没有修改真实 Provider 的 API Key。

## 模型卡

- 执行模型：Sol。
- 推理强度：high。
- 原因：本步骤跨越中转站目录解析、Provider 精确绑定、付费安全、AGENT 执行桥和原生节点恢复。
- 禁止范围：一键复色、Provider 或模型自动切换、fallback、自动重试、Git 提交与推送。

## 已确认并修复的目录根因

1. 旧解析器优先使用 `name_en`，可能把展示字段中的包装字符串当成真实模型 ID。
2. 旧分类器过早信任上游的 `chat` / `text` 标签，导致 `seedance-2.0`、`seedance-2.5`、`gpt-4o-mini-tts`、`suno` 和 `flowmusic` 被放进 LLM。
3. 修复后以真实 `id` 为第一优先级；明确的 image / video / audio 分类仍直接采用；泛化的 chat / text 只在强媒体能力证据检查之后采用。
4. 媒体证据覆盖 Seedance、TTS、Suno、FlowMusic 等模型家族；无法可靠判断的模型仍进入未知分类，不猜测。

## 回归证据

- `canvas-provider-model-classification.test.js`：2/2 通过。
- `canvas-api-model-category-ui.test.js`：2/2 通过。
- `agent-lanvas-v3-generation-round.test.js`：14/14 通过。
- `agent-lanvas-v3-media-execution-service.test.js`：18/18 通过。
- 合计：36/36 通过。
- `git diff --check`：通过；只有工作区既有 LF/CRLF 提示。

## APIMart 现场目录复核

- 使用独立端口运行最新源码，只读取已保存 Provider 的模型目录；没有修改配置或发送生成请求。
- 当前目录总数 292：图片 43、LLM 144、视频 57、音频 44、未知 4。
- `seedance-2.0`、`seedance-2.5` 均进入视频。
- `gpt-4o-mini-tts`、`tts-1`、`suno`、`flowmusic` 均进入音频。
- 以 `[` 开头的包装模型 ID 数量为 0；真实 ID 优先规则在当前目录生效。

## 四类真实测试前置结论

- LLM：已有真实文字 Provider 调用链。
- 图片：已有 APIMart `gpt-image-2` 执行、报价、授权、原生节点和恢复链。
- 视频：已有 APIMart `seedance-2.0` 执行、报价、授权、远端 task ID、原生节点和恢复链。
- 音频：设置页和 GenerationRound 数据结构能识别 `audio`，画布也能表达音频节点；但正式媒体执行服务和原生执行桥仍明确只允许图片、视频。当前不能发送一次可追溯、可恢复的 AGENT 音频真实请求。

## 风险与安全裁决

- “模型目录显示音频”不等于“AGENT 已能生成音频”；不得用目录成功冒充执行成功。
- 在音频执行闭环补齐前，不发送付费音频请求。
- 四类真实测试仍保持每类一次最小请求、明确 Provider / 模型 / 规格、零 fallback、零自动重试；远端结果未知时只记为 `remote-unknown`。

## 下一断点

先设计并实现最小音频执行闭环：APIMart TTS 请求、服务端精确报价与授权、原生音频节点、聊天缩略记录、失败与重启恢复。该闭环本地假 Provider 验证通过后，再按 LLM、图片、视频、音频顺序做四类真实最小测试。

## 阶段 6A：最小音频执行闭环（2026-08-25 16:38 +08:00）

### 已完成范围

- GenerationRound 新增 `audio` 项，仍使用整轮授权、逐项不可变绑定、独立 ToolRun 和当前工作集原生节点。
- 首期只允许已保存的 APIMart `gpt-4o-mini-tts`；固定 WAV，可选受控音色与 0.25–4 倍语速，文本最多 60 字；不接音乐长任务、语音克隆或备用模型。
- 后端使用 `/v1/audio/speech` 同步请求；只接受通过 RIFF/WAVE 头和大小检查的结果，并通过临时文件原子落盘。
- 前端原生任务桥、重启查询、节点完成、聊天音频播放器和多音频折叠均使用同一 AgentSession / ToolRun 真相，不复制媒体。
- 自动模式覆盖图片、视频和音频；手动模式仍是一次 GenerationRound 只确认一次，而不是逐节点确认。

### 失败与恢复边界

- Provider 未收到响应时记为 `remote-unknown`，保留本地任务 ID，只允许查询已有任务，不自动重发。
- Provider 已返回但 HTTP 失败或 WAV 无效时明确记为 `failed`，不伪装为 `remote-unknown`。
- 音频提交失败会把 `local_task_id` 带回原节点恢复清单；WAV 结果按 `.wav` 命名，不伪装成 MP3。
- Provider、模型和规格必须与授权哈希一致；无 fallback、无自动切换、无自动重试。

### 验证证据

- 音频及原生桥定向测试：65/65 通过。
- 模型分类、GenerationRound、媒体服务、原生桥、任务绑定、Session 聊天、授权路由与前端合同合并回归：124/124 通过。
- `canvasRoutes.js`、`agent-native-node-bridge.js`、`smart-canvas-core.js` 语法检查通过。
- `git diff --check` 通过；仅报告工作区既有 LF/CRLF 提示。
- 所有媒体测试均使用假 Provider 和临时输出目录；未调用真实 API、未消耗积分、未修改生产画布数据。

### 仍未完成

- APIMart TTS 的公开精确实时价格当前缺少稳定可核依据；服务端暂用 0.015 美元作为单次授权上限，不把它表述为官方价格。
- LLM、图片、视频、音频四类真实最小请求尚未发送。
- 自动测试不替代浏览器中的节点、聊天回执和重启恢复人工验收。

### 下一断点

在独立测试端口和独立画布只读确认当前已保存的四类 Provider / 模型绑定，然后按 LLM、图片 1K、Seedance 2.0 480P 5 秒、`gpt-4o-mini-tts` 短 WAV 的顺序各发送一次真实请求。每类失败时停止，不自动补发；先核对同一 AgentSession、原生节点、聊天回执、Provider / 模型回执和恢复状态，再决定是否使用用户允许的后续修复测试额度。

## 阶段 6B：四类真实请求与媒体依赖验收（2026-08-25 17:24 +08:00）

### 隔离范围

- 服务端口：3134；画布：`agent-m6-four-types-20260825-1645`。
- 输出根：`D:\Lavans备份\outputs\agent-m6-four-types-real-20260825-1645`。
- Session：`agent-session-1787648884009-9f9e84e6224b`。
- Round：`agent-round-1711ce9b1e52ff7682aaf56ac8715ed18aacb335ed5a96c3`。
- planHash：`6ec04fc3117886a28424fe1daf5610f42d7cc4718ee3ab4c210eff19a5a80121`。
- 未修改或重启用户的 3132 软件实例；真实测试只使用独立画布与独立输出目录。

### 真实请求结果

- LLM：APIMart / `deepseek-v4-pro`，普通聊天成功；assistant 消息持久化 Provider、模型与 usage，重启后仍可追溯。
- 图片：APIMart / `gpt-image-2`，1:1、1K，成功；输出 PNG 为 1254×1254、2,372,581 字节。
- 音频：APIMart / `gpt-4o-mini-tts`，WAV、alloy、1×，成功；输出 24kHz、单声道、16-bit、4.2 秒。
- 视频：APIMart / `seedance-2.0`，9:16、480P、5 秒，成功；输出 MP4 为 496×864、5.042 秒、2,242,901 字节。
- 三媒体 Round 最终状态 `completed`；三个 item 与三个 ToolRun 均为 `succeeded`，重试预算全部为 0。
- 当前工作集正好 3 个节点：图片、音频、视频；聊天记录内三个占位均原位更新为对应媒体缩略结果。
- 图片节点到视频节点存在 1 条连接；视频提交载荷只引用本轮图片，角色为 `first_frame`，并绑定图片 URL、2,372,581 字节和 SHA-256 `8f72b0f11fe4cd01e6541338a3992f4ca3e64ad13e721b64a116b6aa94432c83`。

### 执行中发现并修复

1. 默认画布路由的延迟 Foundation 代理漏转发 `deriveRoundItemReceipt`，导致整轮授权后、Provider 前被安全门拦截。已补一条代理接线；失败轮没有 ToolRun、节点或媒体费用。
2. 新建对话曾未把已保存媒体默认设置快照写入新 Session；已让初始与新建 Session 都原子携带图片、视频、音频设置。
3. 普通 LLM assistant 消息曾未记录 Provider / 模型；已把 `modelBinding` 与 usage 持久化并验证重启恢复。
4. APIMart TTS 返回流式 WAV，RIFF 和 data 长度为 `0xffffffff`。保存前现会按实际字节数规范化头字段；本次隔离文件也已原位只修正头字段，PCM 内容不变。
5. 音频首次完成通知未及时收敛 UI，但持久化本地任务 ID 允许刷新后只读恢复同一成功任务；没有第二次音频提交。随后已把音频查询改为持续查询同一 task ID 直至终态，超时只保留恢复状态，不删除节点、不重发；资源版本已更新，后续无需刷新兜底。

### 安全与费用

- 三媒体整轮服务端预算上限 0.4385 USD：图片 0.0085、视频 0.415、音频安全上限 0.015。
- 一次总确认；Provider / 模型 / 规格锁定；无 fallback、无自动切换、无自动重试。
- 视频轮询始终查询同一远端任务 `task_01M0W3N5C0BTY1ZVGMAVSZEH3P`。
- 失败 Round 和中断事实保留；没有覆盖成功成果，没有向 `remote-unknown` 自动补发。

### 最终回归

- AGENT v3 全部测试与 Canvas Agent Foundation 强制单线程运行：214/214 通过。
- 其中流式 WAV 规范化测试、GenerationRound 单项回执代理测试、图片到视频 first_frame 绑定、音频/视频恢复、一次总确认、零重试和防漂移均通过。
- 自动测试之外，浏览器现场确认右侧聊天窗口与输入框全过程持续存在，图片、音频、视频节点均在原位完成，聊天内均能看到对应媒体结果。

### 下一断点

进入用户人工验收。若用户确认视觉与操作关系通过，再继续剩余 AGENT 开发；若发现界面问题，只围绕具体证据建立新修复项，不重复本轮真实 Provider 请求。
